# Absolute production origin. Do not invent a domain.
# Leave unset in production until the real domain is known.
# Local development can omit this (falls back to http://localhost:3000).
# NEXT_PUBLIC_SITE_URL=https://example.com
